import React, { useEffect, useRef } from 'react'

const PayPal = (props) => {
    const paypal = useRef()

    return (
        <>
            <h2>credit card</h2>
        </>
    )
}

export default PayPal